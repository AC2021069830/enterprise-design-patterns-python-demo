[project]
name = "enterprise-design-patterns-python-demo"
version = "0.1.0"
description = "Enterprise design patterns demo in Python"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
